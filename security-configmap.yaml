apiVersion: v1
data:
  application.yml: |-
    server:
      port: 8085

    spring:
      application:
        name: security-service
      redis:
        host: redis-service.database-namespace.svc.cluster.local
        port: 6379
      logging:
        level:
          root: DEBUG

kind: ConfigMap
metadata:
  name: security-config
  namespace: backend